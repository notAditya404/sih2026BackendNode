PORT=3000
MONGODB_URI=mongodb://127.0.0.1:27017/manova
JWT_SECRET=change-this-to-a-long-random-string
JWT_EXPIRES_IN=7d

# URL of the real ML model service (predict_cli.py's predict_risk(), wrapped
# in a small HTTP service) - leave unset until that's deployed. Until then,
# POST /admin/ml-predictions/recompute returns a clear per-personnel error
# instead of faking a score. See src/services/mlClient.js.
ML_MODEL_URL=
